import itertools

def solve_board(size, endpoints):
    # Returns all valid fully connected paths for a given set of endpoints
    # A path set is valid if all pairs are connected.
    # We want to find endpoints where EVERY valid connection of all pairs ALSO covers all cells.
    pass
